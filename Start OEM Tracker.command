#!/usr/bin/env bash
# Double-click this file in Finder to open the OEM Award Tracker website.
# (macOS may ask permission the first time — right-click → Open to approve.)
cd "$(dirname "$0")"
exec ./run.sh
